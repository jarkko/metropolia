<ul>
  <% @authors.each do |author| %>
  <li id="author_<%= author.id %>">
    <%= author.name %>
    <% form_remote_tag(:url => book_authorships_path(@book)) do %>
      <%= hidden_field_tag "author_id", author.id %>
      <%= submit_tag "Add author" %>
    <% end %>
  </li>
  <% end %>  
</ul>